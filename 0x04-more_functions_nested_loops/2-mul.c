#include "main.h"
#include <stdio.h>
/**
 * mul-multiplies 2 integers
 * @a:integers being multiplied
 * @b:integer being multiplied
 * Return:results of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
