#include "main.h"
#include <stdio.h>
/**
 * _putchar-writes character to std output
 *
 * @c:character input
 *
 * Return:1 is returned on success, -1 for error
 */
int _putchar(char c)
{
	putchar(c);
	return (c);
}
